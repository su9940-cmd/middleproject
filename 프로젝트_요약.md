# 프로젝트 요약

## 한줄 소개
공장 설비 센서 데이터를 이용해 **사고(화재 등) 위험도를 예측하는 머신러닝 중간프로젝트**입니다.
장기적으로는 이 위험 점수에 **LangChain 기반 RAG 에이전트**를 결합해, 위험 상황이 감지되면
설비별 안전 매뉴얼을 자동으로 찾아주는 것을 목표로 하고 있습니다.

## 현재 저장소 상태
- 로컬에 체크아웃된 브랜치는 `data`이며, 원격 `origin/data` 브랜치보다 커밋 1개가 뒤처져 있습니다.
- 실제 전처리/학습 노트북과 가공 데이터는 `origin/data`에만 있고 아직 로컬에 반영(pull/merge)되지
  않은 상태입니다. `main.py`는 `print("Hello from middle-project!")`만 출력하는 자리표시자일 뿐,
  실제 파이프라인이 아닙니다.
- 의존성 관리는 **uv**를 사용합니다 (`uv sync`, `uv run ...`), Python 3.13 기준.

## 데이터
`data/industrial_fire_risk_data.csv` — 약 10만 행, 15분 간격 센서 로그.
컬럼: `Date, Time, Factory, Region, Shift, Workers, Exp, Training, Temp, Pressure, Humidity,
Vibration, Speed, Age, Service_Days, Gas, Sparks, Alarm, Risk, Accident`

원본의 `Accident/Risk/Alarm`은 그대로 라벨로 쓰지 않고, **누출(leakage) 컬럼**으로 취급해 모델
입력에서 제외합니다.

## ML 파이프라인 (`origin/data`의 `industrial_fire_custom_accident_ml.ipynb`)
1. **가상 설비 4종 생성**: 각 공장 데이터를 REACTOR(반응기) / COMPRESSOR(압축기) /
   STORAGE_TANK(저장탱크) / PUMP(펌프) 4개 가상 설비로 나누고, 설비마다 센서값을 다르게 보정해
   설비별로 다른 고장 패턴을 부여합니다.
2. **합성 라벨 `custom_accident` 생성**: 원본 `Accident`가 아니라, 설비 유형별로 다른 변수·상호작용
   조합의 로지스틱 위험 점수로 새 정답 라벨을 만듭니다 (예: 반응기는 온도×압력, 저장탱크는
   가스×스파크가 중요).
3. **시간순 70/15/15 분할**로 train/validation/test를 나눕니다 (셔플하지 않음).
4. **Logistic Regression vs Random Forest**를 비교해 검증 PR-AUC가 더 좋은 모델을 선택합니다.
5. 사고 누락 비용이 크다는 점을 고려해, 검증셋에서 **F2 점수(재현율에 가중치)** 가 최대인 지점을
   경고 임계값으로 선택합니다. 테스트셋은 임계값 선택에는 쓰지 않고 최종 평가에만 사용합니다.
6. 결과물로 `data/processed_industrial_fire_ml.csv`(가공 데이터), `models/*.joblib`(학습된 모델),
   `models/*_metadata.json`(피처 목록/임계값/테스트 성능/설비별 `manual_id`)을 저장합니다. 이 파일들은
   모두 `.gitignore`에 포함되어 있어 커밋되지 않으며, 노트북을 다시 실행하면 재생성됩니다.

## 데모 (`origin/data`의 `mock_safety_alert_demo.ipynb`)
위 파이프라인이 저장한 3개 산출물만 불러와서(재학습 없음), 새로 만든 Mock IoT 이벤트 12건을
`정상 / 주의 / 경고 / 긴급` 4단계로 분류합니다.
- `정상/주의/경고`는 ML 확률과 저장된 `caution_threshold`/`warning_threshold` 비교로 결정합니다.
- `긴급`은 설비별로 하드코딩된 **비상조건**(예: 반응기 온도 42도 이상, 압축기 진동 4.8 이상)으로
  판단하며, ML 판단보다 항상 우선합니다.
- 비정상 상태에는 설비 유형별 체크리스트(한국어)와, 추후 RAG 검색에 쓸 `manual_id`
  (예: `reactor_safety_manual`)를 함께 출력합니다. 다만 실제 매뉴얼 검색(RAG)은 아직 구현되어 있지
  않습니다.

## 앞으로의 방향 (추정)
`pyproject.toml`에 이미 LangChain, Chroma/Pinecone/pgvector, HuggingFace/OpenAI/Ollama, `ragas` 평가
패키지 등 RAG 에이전트에 필요한 의존성이 대부분 들어있습니다. 즉, 다음 단계는 `manual_id`를 키로
설비별 안전 매뉴얼 문서를 검색해주는 RAG 에이전트를 만드는 것으로 보이며, 아직 해당 코드는
작성되지 않았습니다.

## 참고
- `.gitignore`에 `중간프로젝트_지침서.docx`(과제 지침서)와 `dd.ipynb`가 개인 작업 파일로
  제외되어 있습니다.
- 이 문서는 저장소를 분석해 자동 생성한 요약이며, 자세한 개발 규칙은 `CLAUDE.md`를 참고하세요.
